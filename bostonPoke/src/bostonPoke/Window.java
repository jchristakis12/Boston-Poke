package bostonPoke;

import javafx.application.Application;

public class Window implements Display {
		
	public Window(int width, int height, String title, Game game) {
		
		
		/***
		//Stage stage = new primaryStage(title);
		
		Display frame = new primaryStage.setTitle(title);
		
		frame.setPreferredSize(new Dimension(width, height));
		frame.setMaximumSize(new Dimension(width, height));
		frame.setMinimumSize(new Dimesion(width, height));
		
		frame.setResizeable(false);
		frame.setDefaultCloseOperation(frame.EXIT_ON_CLOSE); 
		frame.setLocationRelativeTo(null); //starts in the center of screen instead of top left if was 0
		
		frame.getChildren.add(game); //add game to frame
		frame.setVisible(true);
		game.start();
	}
}
***/
		
	}
}